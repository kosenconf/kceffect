.user-header
  .icon
    = link_to user_icon(user, 48), user
  .main
    %h2
      = user.name
    = link_to "http://twitter.com/#{user.name}", :class => "icon-social", :target => "_blank" do
      %i.icon-twitter-sign
    .profile
      = user.profile
